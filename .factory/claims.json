[
  {
    "id": "static-inventory",
    "claim": "Inventories actions, images, permissions, runner labels, shells, and commands.",
    "where": "landing page and README",
    "test": "npm test -- --grep @claim:static-inventory",
    "sandbox": "bundled sample repository in a fresh CLI process; assert every inventory group"
  },
  {
    "id": "no-workflow-execution",
    "claim": "No workflow steps run.",
    "where": "landing facts, limits, README",
    "test": "npm test -- --grep @claim:no-workflow-execution",
    "sandbox": "temporary repository with a run step that writes a marker; assert the marker is absent"
  },
  {
    "id": "observed-probes",
    "claim": "Observed findings show host probe failures after --probe --sandbox.",
    "where": "landing limits and README",
    "test": "npm test -- --grep @claim:observed-probes",
    "sandbox": "bundled sample repository; empty PATH forces host probe failures; assert observed evidence"
  },
  {
    "id": "report-formats",
    "claim": "Emits terminal, JSON, Markdown, and SARIF reports.",
    "where": "landing page and README",
    "test": "npm test -- --grep @claim:report-formats",
    "sandbox": "bundled sample repository; run and validate every output format"
  },
  {
    "id": "demo-sandbox",
    "claim": "The CLI demo uses bundled sample data and writes a report in a temporary directory.",
    "where": "demo page and README",
    "test": "npm test -- --grep @claim:demo-sandbox",
    "sandbox": "fresh CLI process from a nested fixture repository; compare every path, byte, file type, mode, size, and modification time before and after; parse and read the temporary report"
  },
  {
    "id": "versioned-profiles",
    "claim": "Ships GitHub-hosted, act, generic Linux, and self-hosted Linux profiles.",
    "where": "landing page and README",
    "test": "npm test -- --grep @claim:versioned-profiles",
    "sandbox": "fresh CLI process; list profiles as JSON and validate count and version format"
  },
  {
    "id": "browser-demo-entry",
    "claim": "Try it with sample data opens an isolated report in one click.",
    "where": "landing action and demo banner",
    "test": "npm test -- --grep @claim:browser-demo-entry",
    "sandbox": "fresh 390px browser context; click the landing action once and assert ?demo=1, banner, result, and sample finding inside the first viewport"
  },
  {
    "id": "browser-cli-sample",
    "claim": "The browser demo shows the bundled CLI sample report.",
    "where": "browser demo and README",
    "test": "npm test -- --grep @claim:browser-cli-sample",
    "sandbox": "fresh CLI process and browser context; compare profile, summary, inventory, and findings with the generated browser fixture"
  },
  {
    "id": "terminal-recording",
    "claim": "The landing page records the real CLI demo command, profile, outcome, and temporary report path.",
    "where": "landing terminal recording and README",
    "test": "npm test -- --grep @claim:terminal-recording",
    "sandbox": "fresh CLI demo process; compare its command, target, result, scan count, and temporary report behavior with the generated self-hosted SVG"
  },
  {
    "id": "demo-reset",
    "claim": "Reset demo restores the report and restarts its recording.",
    "where": "browser demo banner and privacy page",
    "test": "npm test -- --grep @claim:demo-reset",
    "sandbox": "fresh browser demo; scroll the isolated report, reset it, assert scroll restoration, visible status, and a restarted row animation"
  },
  {
    "id": "exit-codes",
    "claim": "Exit 0 means portable or warnings only; 1 means nonportable; 2 means invalid input or usage.",
    "where": "README",
    "test": "npm test -- --grep @claim:exit-codes",
    "sandbox": "fresh temporary portable workflow, bundled nonportable sample, and missing path; assert 0, 1, and 2"
  },
  {
    "id": "probe-scope",
    "claim": "The probe checks declared commands and shells, filesystem case behavior, and Docker socket access without running workflow steps.",
    "where": "README",
    "test": "npm test -- --grep @claim:probe-scope",
    "sandbox": "controlled workflow with a marker-writing run step; force missing PATH and assert the four observation classes while the marker remains absent"
  },
  {
    "id": "rule-coverage",
    "claim": "Rules check runner labels, services, containers, OIDC, shells, hosted-only actions, action versions, commands, Docker, and GitHub-hosted paths.",
    "where": "README",
    "test": "npm test -- --grep @claim:rule-coverage",
    "sandbox": "bundled sample against generic profile; assert one rule id for every documented category"
  },
  {
    "id": "site-privacy",
    "claim": "The browser demo makes only same-origin requests and stores no data.",
    "where": "landing facts, demo banner, privacy page",
    "test": "npm test -- --grep @claim:site-privacy",
    "sandbox": "fresh browser context; record requests and inspect localStorage and sessionStorage"
  },
  {
    "id": "no-accounts-or-telemetry",
    "claim": "No accounts or telemetry.",
    "where": "landing facts, README, privacy page",
    "test": "npm test -- --grep @claim:no-accounts-or-telemetry",
    "sandbox": "fresh browser context; visit the landing, demo, privacy, and terms routes; assert the exact landing fact, no account form or authentication controls, no cookies or browser storage, and only same-origin requests"
  },
  {
    "id": "cli-privacy",
    "claim": "The CLI includes no network or telemetry client code.",
    "where": "README and privacy page",
    "test": "npm test -- --grep @claim:cli-privacy",
    "sandbox": "fresh checkout; inspect the dependency manifest and complete Rust source for network or telemetry clients"
  },
  {
    "id": "free-open-source",
    "claim": "Free and open source.",
    "where": "landing facts and README",
    "test": "npm test -- --grep @claim:free-open-source",
    "sandbox": "fresh checkout; assert the repository contains the MIT license grant"
  }
]
